class OcrService {
  Future<String?> extractTextDraft(String localFilePath) async {
    // Fase posterior: ML Kit/OCR local.
    // Regla permanente: lo detectado por OCR nunca alimenta cálculos sin validación humana.
    return null;
  }
}
